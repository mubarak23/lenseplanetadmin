import { Card, CardContent, CardHeader } from '@material-ui/core';

export default () => (
  <Card>
    <CardHeader title='Welcome to the LensPlanet Admin system' />
    <CardContent>Let's get some work done</CardContent>
  </Card>
);
